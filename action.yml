# File generated by github.com/posener/goaction. DO NOT EDIT.

name: poco-github-action
description: Github Action for creating portable application bundles
inputs:
  appEntrypoint:
    default: /bin/sh
    description: "Application container entrypoint"
    required: false
  output:
    default: output
    description: "Output file path"
    required: false
  appDescription:
    default: poco Description
    description: "Application description"
    required: false
  local:
    default: true
    description: "Use local daemon to find image"
    required: false
  checksum:
    default: true
    description: "Generate sha256 checksum"
    required: false
  appName:
    default: poco App name
    description: "Application name"
    required: false
  appCopyright:
    default: noone
    description: "Application copyright"
    required: false
  appAuthor:
    default: noone
    description: "Application author"
    required: false
  appVersion:
    default: "0.1"
    description: "Application version"
    required: false
  appMounts:
    default: /etc/resolv.conf
    description: "Application mounts"
    required: false
  appAttrs:
    default: ipc,uts,user,ns,pid
    description: "Application attrs"
    required: false
  appStore:
    default: .store
    description: "Application store"
    required: false
  image:
    description: "Application image"
    required: false
  compression:
    default: xz
    description: "Application compression"
    required: false
  version:
    default: v0.2.1
    description: "poco version"
    required: false
runs:
  using: docker
  image: Dockerfile
  args:
  - "-appEntrypoint=${{ inputs.appEntrypoint }}"
  - "-output=${{ inputs.output }}"
  - "-appDescription=${{ inputs.appDescription }}"
  - "-local=${{ inputs.local }}"
  - "-checksum=${{ inputs.checksum }}"
  - "-appName=${{ inputs.appName }}"
  - "-appCopyright=${{ inputs.appCopyright }}"
  - "-appAuthor=${{ inputs.appAuthor }}"
  - "-appVersion=${{ inputs.appVersion }}"
  - "-appMounts=${{ inputs.appMounts }}"
  - "-appAttrs=${{ inputs.appAttrs }}"
  - "-appStore=${{ inputs.appStore }}"
  - "-image=${{ inputs.image }}"
  - "-compression=${{ inputs.compression }}"
  - "-version=${{ inputs.version }}"
branding:
  icon: box
  color: purple
